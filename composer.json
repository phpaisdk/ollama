{
    "name": "aisdk/ollama",
    "description": "Official Ollama provider for the PHP AI SDK.",
    "license": "MIT",
    "keywords": [
        "ai",
        "aisdk",
        "ollama",
        "llm",
        "local",
        "openai-compatible"
    ],
    "homepage": "https://github.com/phpaisdk/ollama",
    "support": {
        "issues": "https://github.com/phpaisdk/ollama/issues",
        "source": "https://github.com/phpaisdk/ollama"
    },
    "require": {
        "php": "^8.3",
        "aisdk/core": "^0.8.0",
        "aisdk/openai-compatible": "^0.8.0"
    },
    "require-dev": {
        "laravel/pint": "^1.18",
        "nyholm/psr7": "^1.8",
        "pestphp/pest": "^3.0 || ^4.0",
        "phpstan/phpstan": "^2.0",
        "rector/rector": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "AiSdk\\": "src/Root/",
            "AiSdk\\Ollama\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "AiSdk\\Ollama\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "lint": "vendor/bin/pint --test",
        "format": "vendor/bin/pint",
        "analyze": "vendor/bin/phpstan analyze --memory-limit=512M",
        "refactor:dry": "vendor/bin/rector process --dry-run",
        "refactor": "vendor/bin/rector process",
        "test": "vendor/bin/pest",
        "test:all": [
            "@lint",
            "@analyze",
            "@test"
        ]
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "php-http/discovery": true
        }
    }
}
